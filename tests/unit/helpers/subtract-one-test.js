import {
  subtractOne
} from '../../../helpers/subtract-one';
import { module, test } from 'qunit';

module('SubtractOneHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = subtractOne(42);
  assert.ok(result);
});
