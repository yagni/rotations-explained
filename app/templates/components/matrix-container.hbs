{{#each matrix in matrices}}
  {{#if matrix.isFirst}}
    {{matrix-4d matrix=matrix allowClose=false close="deleteMatrix"}}
  {{else}}
    {{matrix-4d matrix=matrix allowClose=true close="deleteMatrix"}}
  {{/if}}
{{/each}}
